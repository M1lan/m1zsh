# ~/.config/m1zsh/personal.zsh
# This file is intentionally outside git. Put private paths, accounts, secrets,
# machine-specific aliases, and local tool preferences here.

# Example:
# export EDITOR='emacsclient -n -a vi'
# path=("$HOME/private/bin" $path)
