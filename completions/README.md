# Completions

Put public, generated completions here only when they are safe to publish and
have regeneration provenance.

Private or machine-specific completions belong in a personal overlay directory,
not this repository.
